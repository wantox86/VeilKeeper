.env

# Go
/backend/veilkeeper-api
/backend/*.test

# Android / Gradle
android/.gradle/
android/.kotlin/
android/local.properties
android/.idea/
android/*.iml
android/app/build/
android/build/
android/captures/
android/.cxx/

# Data (local dev only, not app source)
/data/attachments/*
!/data/attachments/.gitkeep

# OS
.DS_Store

# Self-signed TLS cert/key for web's LAN HTTPS deployment (Web Sprint 8
# follow-up) -- generated locally via web/nginx/certs/generate-cert.sh,
# never committed. See CLAUDE.md "Web Sprint 8" for why this exists.
web/nginx/certs/*.pem
