# Copy this file to .env and fill in real values for local development.
# NEVER commit the real .env file (it's already in .gitignore).

# --- API ---
HTTP_PORT=8080

# --- MySQL ---
DB_HOST=mysql
DB_PORT=3306
DB_NAME=veilkeeper
DB_USER=veilkeeper
DB_PASSWORD=changeme-dev-password
DB_ROOT_PASSWORD=changeme-dev-root-password
