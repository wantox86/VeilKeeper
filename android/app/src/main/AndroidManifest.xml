<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".VeilKeeperApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.VeilKeeper"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">

        <!--
          android:allowBackup="false" is intentional: this app will hold a
          zero-knowledge-encrypted vault. Auto-backup of app data (which can
          include Keystore-wrapped key material depending on OEM backup
          implementation) is disabled by default until Sprint 3 defines an
          explicit backup policy.

          android:usesCleartextTraffic="true" is a Sprint 1 homelab-dev-only
          concession: the backend has no TLS termination yet (SPEC-BASE.md
          Section 30 explicitly scopes "HTTPS" to "non-local production
          environments"). This must be revisited (removed, or scoped to a
          dev-only build variant / network security config allowlisting only
          the homelab's LAN range) before any real production deployment.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.VeilKeeper">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
